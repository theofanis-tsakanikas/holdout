# Changelog

Dated entries, newest first. The running record is [`PLAN.md`](PLAN.md)'s *Progress* sections and
[`TASKS.md`](TASKS.md); the ledger of what was decided and deferred is
[`docs/DECISIONS.md`](docs/DECISIONS.md); what was found wrong and where it stands is
[`docs/FINDINGS.md`](docs/FINDINGS.md). This file is the short form.

## 2026-09-13 — the estate produced the number

A fresh-context review found that the estate's refusal, accepted as the phase-3 demonstration,
was a correct output of a ledger nobody had priced: the ERP's history was exported once per slice
on its last day, so no earlier sale had a cost. Exported daily, the coefficient of variation went
from 3.57 to 0.12 and the experiment exists. Eight branches from that review landed in a day;
the cycle that followed read out **+12,398 cents [+11,223, +13,571], p = 0.001** for the sized
experiment and refused the peeking one, and `destroy all` left the account clean. Also: the
runner reads `result_state`; the readout appends and restates and runs the compiled pinned query;
a second budget sees the Databricks line; the reaper is armed by default, deletes by the handle
the API takes and is heard; every action is pinned; the deploy role cannot grow itself; every
event stream is deduplicated; `destroy` gives its verdict after the cleanup.

## 2026-09-12 — the screens draw

`inspect`, the sixth workflow: every table, both readouts, the door tried, every dashboard dataset
executed as the dashboard executes it, the demo's queries. Its first honest run was red four ways
two green `run`s had not seen — the readout screen read the type's columns while the table had the
pipeline's, six undeclared parameters, a monitor reading a table nothing wrote, a default catalog
of `hive_metastore`. The decision record reached gold. First `destroy all` to pass end to end.

## 2026-09-11 — phase 4 opened

The agent surface (`src/holdout/agent/`): a registry that is the limit, a seven-field proposal,
ceilings in a sixth contract family, a recording fingerprinted over registry and prompt. Claim 6
built: N, M and K from a real model's proposals graded live, then K under peeking and post-hoc
exclusions on the null world. The phase-3 integration review.

## 2026-09-08 → 10 — phase 3

`deploy`, `backfill`, `run`, `destroy` written and run; the estate loads `estate` scale after the
scenario's world could not carry an experiment; the registered model emptied before `ml` is
destroyed; the Databricks-managed VPC removed by the workflow under a tag-then-delete statement.

## 2026-09-02 → 06 — phase 2

Ingest with the ERP's successive drops, silver with as-of joins and quarantine, gold through dbt,
the training job with claim 4's correction and five promotion gates, the assignment table
append-only, the compiled dashboards, `infra/bootstrap` and `infra/foundation` without a VPC.

## 2026-08-27 → 31 — phase 1

The contract layer and its compilers; the core — guardrails, the ladder, the design engine,
stratified assignment, the four checks, the design-based estimator, the censoring correction; the
six adversarial worlds behind an enforced import barrier; claims 1, 2, 3, 4 and 7 with their
mutations; `gate-proof`; the findings, expiry, figures and language gates; the phase-1 review.
