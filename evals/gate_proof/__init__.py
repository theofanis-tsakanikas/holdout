"""`make gate-proof` — break each gate on purpose and demand a refusal by name.

`engine.py` states the three rules the harness enforces and where the planter's
independence from the detector actually lies. `mutations/claim-N/` holds one YAML per
planted break, each naming the eval check that must refuse it.
"""
