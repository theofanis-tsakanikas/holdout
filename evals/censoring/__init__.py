"""Claim 4 — a stock-out is never read as zero demand.

Run it: `python -m evals.censoring` — or `make claim-4`, which also plants the mutations that
prove each gate bites. `README.md` in this directory states what is attacked, where the
independence lives and what the eval does not prove.
"""
