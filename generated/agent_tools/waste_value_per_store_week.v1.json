{
  "$comment": "GENERATED FILE — DO NOT EDIT. source: contracts/metrics/waste_value_per_store_week.v1.yaml; generator: holdout.contracts.compilers.agent_tool; regenerate: make contracts.",
  "name": "metric_waste_value_per_store_week",
  "description": "The cost of everything disposed of, per store, ISO week and category, valued at the cost as it was known on the day of disposal. Returns the metric at grain (store_id, iso_week, category) in EUR, rounded half_even to 2 decimals. The definition is fixed by contract; this tool does not accept a formula.",
  "input_schema": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "date_from",
      "date_to"
    ],
    "properties": {
      "date_from": {
        "type": "string",
        "description": "ISO date, inclusive. The metric version in force on this date applies."
      },
      "date_to": {
        "type": "string",
        "description": "ISO date, exclusive."
      },
      "store_id": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Restrict to these store_id values. Omit for all."
      },
      "category": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Restrict to these category values. Omit for all."
      },
      "experiment_id": {
        "type": "string",
        "description": "Optional. Restrict to the units assigned to this experiment and return the result split by arm. Results are refused before the declared end date."
      }
    }
  },
  "metric": {
    "id": "waste_value_per_store_week",
    "version": 1,
    "effective_from": "2025-09-01",
    "grain": [
      "store_id",
      "iso_week",
      "category"
    ],
    "unit": "EUR",
    "rounding": {
      "mode": "half_even",
      "decimals": 2
    },
    "canonical_integer_scale": 100
  }
}
